.DS_Store
*.log
node_modules/

# Boilerplate developer's local stack state (not for distribution)
.env
.dify-version
vendor/dify/

# Local memory state (only the placeholders are tracked)
daily/*
!daily/.keep
knowledge/*
!knowledge/.keep
state.json
state.json.compile-log
