.DS_Store
*.log
node_modules/

# Boilerplate developer's local stack state (not for distribution)
.env
.dify-version
vendor/dify/

# Operational state files (ops only, not memory content)
.compile-state.json
.compile-state.json.log
.compile.lock
.flush-state.json
.flush.log
